export const meta = {
  title: 'Example Post',
  description: 'An example post written in MDX',
  collection: 'posts',
  date: '2020-11-04'
}

# Your example blog post

You can write your posts and pages in either MDX or JS.

If you remove the `collection` and `date` from `meta` above, this page will be removed from your `posts` collection and just becomes a normal a page.

import island from '../images/island.jpg'

<img src={island} alt='MDX is pretty cool...' width={400} />
